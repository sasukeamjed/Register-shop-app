import 'package:flutter/material.dart';

class AdminExtraPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Extra Page', style: TextStyle(fontSize: 36, color: Colors.blue)),
    );
  }
}
